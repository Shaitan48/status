-- =============================================================================
-- Файл: 001_system_events.sql
-- Назначение: Запись начальных системных событий.
-- =============================================================================

-- Комментарий: Запись события об успешном завершении инициализации базы данных.
INSERT INTO system_events (event_type, severity, message, source) VALUES
('DB_INIT', 'INFO', 'База данных успешно инициализирована (Структура v4.3.0).', 'db_seed_data')
ON CONFLICT DO NOTHING; -- Не вставлять повторно при перезапуске

-- =============================================================================
-- == КОНЕЦ ЗАПИСИ ОБЩИХ СОБЫТИЙ ==
-- =============================================================================